enum user_role {
  user
  admin
  super_admin
}

enum admin_role {
  super_admin
  admin
}

enum user_status {
  active
  in_active
  banned
  deleted
}

enum gender {
  male
  female
}

enum category_status {
  active
  in_active
}

enum event_status {
  active
  in_active
}

enum event_type {
  public
  private
}

enum category_type {
  public
  private
}
