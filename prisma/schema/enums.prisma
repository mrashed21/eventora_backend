enum user_role {
  user
  admin
  super_admin
}

enum admin_role {
  super_admin
  admin
}

enum user_status {
  active
  in_active
  banned
  deleted
}

enum gender {
  male
  female
}

enum category_status {
  active
  in_active
}

enum event_status {
  active
  in_active
}

enum event_type {
  public
  private
}

enum category_type {
  public
  private
}

enum participation_status {
  pending
  approved
  rejected
  cancelled
}

enum payment_status {
  not_required
  pending
  paid
  failed
  refunded
  cancelled
}

enum payment_provider {
  stripe
}

enum invitation_status {
  pending
  accepted
  rejected
  cancelled
}
